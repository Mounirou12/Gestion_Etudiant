<form method="post" action="{{ path('app_gestu_etud_delete', {'id': gestu_etud.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ gestu_etud.id) }}">
    <button class="btn btn-primary">Delete</button>
</form>
