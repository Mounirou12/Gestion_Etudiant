{% extends 'base.html.twig' %}

{% block title %}Hello ProfilController!{% endblock %}

{% block body %}

<section class="vh-100" style="background-color: #eee;">
  <div class="container py-5 h-100">
    <div class="row d-flex justify-content-center align-items-center h-100">
      <div class="col-md-12 col-xl-4">

        <div class="card" style="border-radius: 15px;">
          <div class="card-body text-center">
            <div class="mt-3 mb-4">
              <img src="{{asset("/profilAdmin.png")}}"
                class="rounded-circle img-fluid" style="width: 100px;" />
            </div>
            <h4 class="mb-2">{{ app.user.email }}</h4>
            <p class="text-muted mb-4">Administrateur <span class="mx-2">|</span> <a
                href="#!">ESGIS BENIN</a>
            </p>
            
            <a href="{{path("app_gestu_etud_index")}}" class="btn btn-primary btn-rounded btn-lg">
              Dashboard
            </a>
            <a href="{{path("app_logout")}}" class="btn btn-primary btn-rounded btn-lg">
              Log out
            </a>
            
        </div>

      </div>
    </div>
  </div>
</section>
{% endblock %}
